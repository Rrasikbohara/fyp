import { api } from '../services/api'; 

const backendUrl = import.meta.env.VITE_API_URL || 'http://localhost:3000'; // Ensure default value

// Image uploading functionality removed.
export const uploadImage = () => {
  // This function is no longer used.
};
